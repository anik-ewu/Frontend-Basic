import { HilightDirectiveWithRendererDirective } from './hilight-directive-with-renderer.directive';

describe('HilightDirectiveWithRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightDirectiveWithRendererDirective();
    expect(directive).toBeTruthy();
  });
});
