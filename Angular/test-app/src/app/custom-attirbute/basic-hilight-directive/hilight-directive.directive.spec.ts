import { HilightDirectiveDirective } from './hilight-directive.directive';

describe('HilightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
